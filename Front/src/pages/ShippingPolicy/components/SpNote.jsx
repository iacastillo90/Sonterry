import React from 'react';

const SpNote = () => (
  <div className="sp-note">
    <strong>⚠️ Importante sobre el domicilio</strong>
    El costo del envío con la empresa de mensajería o domiciliarios corre por cuenta del comprador.
    Te informaremos el valor antes de confirmar el despacho para que no haya sorpresas.
  </div>
);

export default SpNote;
